import {expect} from 'chai'
import {calculate} from '../../src/fraction'
// import { expect } from 'chai'


describe('It', () => {
    describe('should', () => {
        it('fail', () => {
            expect(calculate()).to.eql(2)
        })
    })
})
